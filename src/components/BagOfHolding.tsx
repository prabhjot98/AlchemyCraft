import { type JSXElement } from 'solid-js'
import { type Inventory } from '../types/inventory'

export const BagOfHolding = (props: { inventory: Inventory }): JSXElement => {
  return <div>Bag of holding goes here{}</div>
}
